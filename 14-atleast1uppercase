# take a string from the user and check contains atleast one capital letter or not?

username = input (" enter your name: ")

def hasup(username):
    return any(char.isupper() for char in username)

if (hasup(username)) == True:
    print ("username good, as it has atleast 1 capital letter")
else:
    print ("username name bad, it has to have atleast 1 uppercase letter")
