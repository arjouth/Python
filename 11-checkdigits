#  take a string from the user and check contains atleast one digit or not?


def hasnum(str):
    return any(char.isdigit() for char in str)

str = input ("enter a value : ")

if hasnum(str) == True:
    print ("this sentence has atleast 1 digit")
elif hasnum(str) == False:
    print("this sentence has no digit")

