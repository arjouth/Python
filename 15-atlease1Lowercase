# take a string from the user and check contains atleast one small letter or not?

username = input (" enter your name: ")

def haslo(username):
    return any(char.islower() for char in username)

if (haslo(username)) == True:
    print ("username good, as it has atleast 1 lowercase letter")
else:
    print ("username name bad, it has to have atleast 1 lowercase letter")
