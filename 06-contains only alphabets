# take a string from the user and check contains only alphabets or not?

str = input("enter a value : ")

if str.isalpha():    
  print ("entered", str, "has only alphabets")
else:    
  print ("entered", str, "does not have only alphabets")
