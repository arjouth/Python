# take a string from the user and check contains atleast one alphabets or not?


alpha = input ("enter what's on your mind : ")

def hasalp(alpha):
    return any(char.isalpha() for char in alpha)

if (hasalp(alpha)) == True:
    print ("entered statement has atlease 1 alphabet")
else:
    print ("entered sentence has no alphabets")
