# Show the below menu to the user and until user select quit and display corresponding os message
'''
Menu:
1. windows
2. Linux
3. Mac
4. quit
'''

print ("1. Windows")
print ("2. Linux")
print ("3. Mac")
print ("4. Quit")

choice = input('Enter your choice of OS [1-3] to exit choice [4] : ')

choice = int(choice)

if choice == 1:
        print ("your choice of OS selected is Windows")
elif choice == 2:
        print ("your choice of OS selected is Linux")
elif choice == 3:
        print ("your choice of OS selected is Mac")
elif choice == 4:
        print ("exiting ......")
else:
        print ("Invalid number. Try again...")
